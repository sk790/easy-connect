{
    "version": 2,
    "builds": [
      {
        "src": "src/**/*",
        "use": "@vercel/node"
      }
    ],
    "buildCommand": "npm install --legacy-peer-deps && npm run build",
    "outputDirectory": "dist"
  }
  